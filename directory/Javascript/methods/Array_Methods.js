let arr = [1,2,3,4,5,6,7,8,9];
arr.push(12); //add one or more elements
arr.pop(12);  // remove the last elements
arr.shift()   // removes element at the beginning
arr.unshift(1); //add one or more elements at the beginning
arr.splice(1,2) //remove the elements 1=start 2=delet count
arr.splice(1,0,2,3) //2,3= items to be added
arr.indexOf(3)


console.log(arr);
